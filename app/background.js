// chrome.browserAction.onClicked.addListener(function (tab) {
//   var url = "pandler-test1://" + tab.url.split("://")[1];
//   alert(url);
//   chrome.tabs.sendMessage(tab.id, tab.url)
//   // chrome.tabs.update(tab.id, {url: url});
// });
